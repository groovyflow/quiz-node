Simple-minded questionnaire using Node, jQuery and Handlebars

Install Node.js

On the command prompt, type:
npm install      //only the first time you run, or whenver you make a change to package.json
npm server.js

